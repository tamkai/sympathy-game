---
description: 要件に基づいてコードを実装する
---
1. **Load Context**: `.agent/agents/coder.md` と `.agent/rules.md` を読み込む。
2. **Read Specs**: `docs/requirements.md` と `docs/decisions.md` を読む。
3. **Implement**: `src/` にコードを記述する。
4. **Test**: `tests/` にテストを記述し、実行する。
